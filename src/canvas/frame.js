/** Design frame from both SVGs: viewBox="0 0 84 68" */
export const SVG_FRAME = { width: 84, height: 68 };

/** Upscale for on-screen preview while keeping vector rasterization sharp. */
export const RENDER_SCALE = 4;

export const SVG_PATHS = {
  frame: "/betspot-frame.svg",
  overlay: "/betspot-overlay.svg",
  plasma: "/plasma.svg",
};
